{{- if .Values.actuator.metrics.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "cfe-crime.fullname" . }}-monitoring
  labels:
    {{- include "cfe-crime.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "cfe-crime.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
      - namespaceSelector:
          matchLabels:
            component: monitoring
{{- end }}