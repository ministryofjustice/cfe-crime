apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cfe-crime.fullname" . }}-test-connection"
  labels:
    {{- include "cfe-crime.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cfe-crime.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
